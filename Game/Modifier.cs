public struct Modifiers
{
    public bool AutoPlay;
    public float TimeRate;

    public Modifiers(bool autoPlay, float timeRate)
    {
        AutoPlay = autoPlay;
        TimeRate = timeRate;
    }
}
